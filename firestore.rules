
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- Helper Functions ---
    function isAdmin() {
      // Ensures the user is authenticated and their role in the 'users' collection is 'admin'.
      return request.auth != null && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    // --- Publicly Readable Collections ---
    match /locations/{locationId} {
      allow get, list: if true;
      allow write: if isAdmin();
    }
    
    match /spots/{spotId} {
      allow get, list: if true;
      allow write: if isAdmin();
    }
    
    match /pricingTiers/{tierId} {
      allow get, list: if true;
      allow write: if isAdmin();
    }

    // --- User Profiles ---
    match /users/{userId} {
      // Anyone can read a user's public profile data (name, photo)
      allow get: if true;
      
      // Only admins can list all users
      allow list: if isAdmin();

      // Anyone authenticated can create their own profile document. Admins can create any.
      allow create: if request.auth != null;

      // Users can update their own document. Admins can update any.
      allow update: if isOwner(userId) || isAdmin();
      
      // Only admins can delete user documents.
      allow delete: if isAdmin();
    }

    // --- Kiosk-related Collections ---
    match /kioskSettings/{settingId} {
      allow read, write: if isAdmin();
    }
    
    match /kioskDevices/{deviceId} {
      // Admins can read the status of all devices.
      allow read: if isAdmin();
      // Any client (even anonymous kiosks) can write their own heartbeat.
      allow write: if true;
    }

    match /kioskControl/{controlId} {
      // Only admins can write commands.
      allow write: if isAdmin();
      // Any client (kiosks) can read control commands.
      allow read: if true;
    }

    match /kioskEvents/{eventId} {
      // Anyone (especially kiosks) can create events. Events are write-only for security.
      allow create: if true;
      allow read, update, delete: if false;
    }

    match /kioskStats/{locationId} {
      // Anyone can read stats.
      allow read: if true;
      // Allow any client to write stats (throttled on the client-side).
      allow write: if true; 
    }
  }
}
